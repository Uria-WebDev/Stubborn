{% extends 'base.html.twig' %}

{% block title %}Stubborn - Ajouter un Produit{% endblock %}

{% block admin %}
<h1>Produit</h1>

{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.price) }}
    {{ form_row(form.image) }}

    <button class="btn btn-primary mt-2">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('app_admin') }}" class="btn btn-secondary mt-2">Retour</a>
{% endblock %}